listen_addr = "0.0.0.0:50061"
control_plane_addr = "http://127.0.0.1:9090"
# Apple Silicon prefers MLX automatically. Intel macOS falls back to llama.cpp when available.
runtime_backend = "auto"
models_root = "./models"
heartbeat_interval_seconds = 15
